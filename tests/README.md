# Tests

Unit and integration tests for ECS and engine features.

## Structure

- `ecs/` — core ECS validation
- `features/` — feature-specific tests
- `integration/` — full-system scenarios

## Notes

Tests are critical for validating ECS invariants and system correctness.